const DEFAULT_LANGUAGE = 'tr';

const APP_NAME = 'DENEME SPRING BOOT APP';
const APP_URL = 'http://localhost:3000/';
const APP_VERSION = '1.0.0';

const API_URL = 'http://localhost:8080/api/v1';

const COOKIE = {
    AUTHENTICATION: 'authentication',
    LANGUAGE: 'language',
};

const ROUTES = {
    HOME: '/',
    ABOUT: '/about',
    AUTHORS: '/authors',
    BOOKS: '/books',
    CITIES: '/cities',
    DEFAULT: '/authors',
};

export {
    DEFAULT_LANGUAGE,
    APP_NAME,
    APP_URL,
    APP_VERSION,
    API_URL,
    ROUTES,
    COOKIE,
}
